# Environment Variables

## Required Environment Variables

### N8N_WEBHOOK_URL
The URL of your N8N webhook endpoint that will process the URLs.

**Example:**
```
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/process-url
```

## Setting Environment Variables

### For Local Development
Create a `.env.local` file in the root directory:

```bash
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/process-url
```

### For Vercel Deployment
1. Go to your Vercel project dashboard
2. Navigate to Settings > Environment Variables
3. Add the following variable:
   - **Name:** `N8N_WEBHOOK_URL`
   - **Value:** Your N8N webhook URL
   - **Environment:** Production, Preview, Development

## N8N Webhook Response Format

Your N8N webhook should return a **binary file** directly (e.g., a text file).

### Expected Response:
The webhook should use "Respond to Webhook" node with:
- **Respond With:** "Binary File"
- **Response Data Source:** "Choose Automatically From Input"

### Optional: Setting Filename
To control the downloaded filename, add a "Set" node before "Respond to Webhook" to set headers:
- **Header:** `Content-Disposition`
- **Value:** `attachment; filename="your-filename.txt"`

## Security Considerations

1. **HTTPS Only:** Ensure your N8N webhook URL uses HTTPS
2. **Authentication:** Consider adding authentication to your N8N webhook
3. **Rate Limiting:** Implement rate limiting to prevent abuse
4. **File Cleanup:** Set up automatic file deletion after 24 hours

